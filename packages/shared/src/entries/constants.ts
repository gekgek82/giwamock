/**
 * Browser-safe entry: protocol constants and API route literals only.
 * Import from `@giwater/shared/constants` in Next.js — avoids the main barrel (DTOs, etc.).
 */
export * from '../constants';
